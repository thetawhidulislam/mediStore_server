{
    "version": 2,
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install",
    "builds": [{
        "src": "api/index.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/api/index.js"
    }]
}